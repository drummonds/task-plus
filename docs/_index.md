Companion documentation site for [task-plus](https://github.com/drummonds/task-plus),
maintained in a separate [task-plus-docs](https://github.com/drummonds/task-plus-docs) repository.

Go CLI tool that standardizes common development workflows across repositories.
Subcommand architecture: `release`, `pages`, `wt` (worktrees), `claude`, and `md2html`.
