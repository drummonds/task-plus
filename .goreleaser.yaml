version: 2
project_name: task-plus

before:
  hooks:
    - go mod tidy

builds:
  - id: task-plus
    main: ./cmd/task-plus
    binary: task-plus
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
  - id: tp
    main: ./cmd/tp
    binary: tp
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64

archives:
  - formats:
      - tar.gz
    builds:
      - task-plus
      - tp

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
